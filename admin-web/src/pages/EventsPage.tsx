import PlaceholderPage from '../components/PlaceholderPage';

export default function EventsPage() {
  return (
    <PlaceholderPage
      title="Upcoming Events"
      description="Event posters need Firebase Storage. For this demo, events stay as bundled images in the app."
    />
  );
}
