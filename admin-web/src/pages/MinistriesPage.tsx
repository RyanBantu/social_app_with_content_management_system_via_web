import PlaceholderPage from '../components/PlaceholderPage';

export default function MinistriesPage() {
  return (
    <PlaceholderPage
      title="Ministries"
      description="Ministry images and timings are not editable in this demo. Only Daily Devotion updates live via Firestore."
    />
  );
}
