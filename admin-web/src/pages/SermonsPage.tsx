import PlaceholderPage from '../components/PlaceholderPage';

export default function SermonsPage() {
  return (
    <PlaceholderPage
      title="Sermon Notes"
      description="Sermon uploads need Firebase Storage. For this demo, sermon files stay bundled in the app."
    />
  );
}
